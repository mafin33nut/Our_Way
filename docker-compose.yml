services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    env_file:
      - .env
    ports:
      - "21772:8000"
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile.prod
      args:
        VITE_API_URL: https://our-way.jkproduction.pro:8000
    ports:
      - "21771:80"
    depends_on:
      - backend
    restart: unless-stopped
